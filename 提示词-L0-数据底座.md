# Codex 任务：实现 CodePick · L0 数据底座（独立开发，最后联调）

## 角色
你是资深 Python 数据工程师，独立负责实现 CodePick 平台的 **L0「数据底座」**：负责从外部源采集内容、尽可能完整地不可变保存、规范化、去重、并发出事件给上层。

## 背景（重要）
CodePick 是一个分层数据平台，共 4 层：**L0 数据底座 → L1 数据加工 → L2 判断与智能体 → L3 应用与分发**。
这 4 层由不同会话**并行开发**，**最后统一联调**。你只负责 L0。其他层会以 stub 方式对接你，因此你必须**严格遵守对外契约**，联调时它们会把 stub 换成你真实的实现。

## 输入材料（本文件夹 4 份 HTML，先全部读完再动手）
- `L0-数据底座-产品文档.html` —— 目标、全量保存原则、边界、对外契约
- `L0-数据底座-架构设计.html` —— 三段存储、数据模型、去重、可重放血缘
- `L0-开发Plan-详尽版.html` —— EPIC/Feature 拆解 + 每项「验收标准」+「自测」（实现依据）
- `L0-实施手册-交付包.html` —— 仓库脚手架、可执行 DDL、接口契约、docker-compose、冒烟自检

## 任务
按《开发Plan》的 EPIC **E0→E8**、Feature 顺序实现，**逐个用其「验收标准」+「自测」收口**；
按《实施手册》的脚手架、DDL、接口契约、docker-compose 落地。

## 硬约束（不可违背）
1. **信条**：尽可能保存所有信息。原始层 `raw_documents` 不可变、只追加、永不删除；`RawStore` 仓储**不得有 update/delete** 方法。
2. **三段存储**：原始层(raw_documents + 对象存储) / 规范层(content_items) / 索引层，强血缘可溯源。
3. **可重放**：能从原始层重建规范层，无需重抓外部源。
4. **去重保留变体**：去重只在规范层选"代表"，原始抓取各自留痕，绝不丢数据。
5. **事件用事务性 outbox**：`content.ingested` 不丢不重。
6. **对外契约冻结**（上层 L1 唯一依赖，不可改）：
   - 事件 `content.ingested {content_id, lang}`
   - 查询 `list_contents(status, since, limit, cursor)` / `get_content(id)`
   - 稳定字段：id / canonical_url / title / clean_text / lang / source / published_at / status
   - 表归属：L0 拥有 `sources / source_health / crawl_jobs / raw_documents / content_items / content_versions`
7. **依赖方向**：`core_data` 不得 import 任何上层（L1/L2/L3）。
8. **技术栈**：Python 3.12 / Playwright / feedparser / PostgreSQL + pgvector / SQLAlchemy + Alembic / Redis + Arq / S3 兼容对象存储。

## 交付标准（DoD）
- 每个 Feature 通过其「自测」；每个 Sprint 末 `make smoke` 输出 `L0 PIPELINE: PASS`。
- 覆盖率 ≥ 80%，ruff / mypy / CI 全绿。
- 满足《实施手册》第 7 节 L0 DoD 全部条目（含：自动运行≥24h、全量保存可校验、可重放、事件不丢不重）。
- 产出：可运行代码 + 测试（含 fixtures）+ Alembic 迁移 + docker-compose + README（运行/测试命令）。

## 工作方式
- 每完成一个 Sprint 提交一次，附：已实现 Feature 列表、自测结果、覆盖率、遗留项。
- **联调预留**：对外契约（事件 + 查询 + 表归属）保持稳定，方便后续 L1 接入。
- 遇到契约层面的歧义**先停下提问**，不要擅自更改冻结契约。
